# Environment — never commit real credentials
.env
.env.local
.env.*.local
# .env.docker is intentionally tracked (contains only placeholders)

# TLS certificates — never commit private keys
certs/

# Dependencies
node_modules/

# Logs
npm-debug.log*
*.log

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
